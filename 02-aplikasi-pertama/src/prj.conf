# Konfigurasi minimal, cukup pakai default Zephyr untuk console UART.
# CONFIG_PRINTK dan CONFIG_SERIAL sudah aktif secara default lewat
# board default config, tidak perlu diaktifkan manual di sini.
